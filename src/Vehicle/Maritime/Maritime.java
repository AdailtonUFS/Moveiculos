package src.Vehicle.Maritime;

import src.Vehicle.Vehicle;

/**
 * Maritime
 */
public class Maritime extends Vehicle{
    public Maritime(String code, String name, String color, Double height, Double width, Double velocity){
        super(code, name, color, height, width, velocity);
    }
}